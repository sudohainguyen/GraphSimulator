<UserControl x:Class="GraphSimulator.User_Controls.Node"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GraphSimulator.User_Controls"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">
    <Grid DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:Node}}}"
          Height="{Binding Diameter}"
          Cursor="Hand">
        <Grid.Background>
            <VisualBrush>
                <VisualBrush.Visual>
                    <Ellipse Height="{Binding Diameter}"
                             Width="{Binding Diameter}"
                             Fill="{Binding FillBrush}"
                             Stroke="{Binding StrokeBrush}"/>
                </VisualBrush.Visual>
            </VisualBrush>
        </Grid.Background>
        <TextBlock Text="{Binding Identifier}" 
                   Foreground="White"
                   FontSize="14"
                   FontWeight="DemiBold"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   Width="{Binding Diameter}">
        </TextBlock>
    </Grid>
    

</UserControl>
