<Window x:Class="GraphSimulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GraphSimulator"
        xmlns:usercontrol="clr-namespace:GraphSimulator.User_Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0">
            <TabItem Header="Create graph">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="4*"/>
                    </Grid.ColumnDefinitions>
                    
                </Grid>
            </TabItem>
            <TabItem Header="Run algorithms">
                
            </TabItem>
        </TabControl>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <Button Click="Button_Click">Change color</Button>
                <ToggleButton Name="ButtonAddConn"
                          Checked="ButtonAddConn_Checked"
                          Unchecked="ButtonAddConn_Unchecked">Add connection</ToggleButton>
            </StackPanel>
            <Canvas Background="LightGray"
                        Grid.Column="1"
                        x:Name="GraphContainer"
                        Panel.ZIndex="-999"
                        Focusable="True"
                        KeyDown="GraphContainer_KeyDown"
                        MouseLeftButtonDown="GraphContainer_MouseLeftButtonDown">
                <usercontrol:Connection X1="10" Y1="200" Stroke="Black" />
            </Canvas>
        </Grid>
    </Grid>
</Window>
